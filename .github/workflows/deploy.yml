name: Build and Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Permet de lancer manuellement

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Environment Variables
        run: |
          echo "Checking if required secrets are set..."
          if [ -z "${{ secrets.VITE_SUPABASE_URL }}" ]; then
            echo "❌ ERROR: VITE_SUPABASE_URL secret is not set"
            echo "Please set it in: https://github.com/everssteeve/BoardInfluence/settings/secrets/actions"
            exit 1
          fi
          if [ -z "${{ secrets.VITE_SUPABASE_ANON_KEY }}" ]; then
            echo "❌ ERROR: VITE_SUPABASE_ANON_KEY secret is not set"
            echo "Please set it in: https://github.com/everssteeve/BoardInfluence/settings/secrets/actions"
            exit 1
          fi
          echo "✅ All required secrets are set"
          echo "VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}"
          echo "VITE_SUPABASE_ANON_KEY: [REDACTED]"
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build application
        run: npm run build
        env:
          NODE_ENV: production
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_APP_URL: https://everssteeve.github.io/BoardInfluence
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
  
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
